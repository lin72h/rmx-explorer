# op-116-cont mx-a64z asl-harness macOS-truth (UNIFIED harness, like-for-like)
# host:      mm4.local
# os:        macOS 27.0 (Darwin 27.0.0)  arch arm64 (Apple M4)
# asl:       Apple system ASL (libSystem; API deprecated since macOS 10.12 -> os_log)
# syslogd:   com.apple.system.logger (pid 566), Mach bootstrap
# compiler:  Apple clang 17.0.0
# harness:   asl-harness.c  git blob 34775198d35eab94e09b69a6373beaebc40fc31d  (UNIFIED @8750b46;
#            rx-x64z ran the IDENTICAL blob -> apples-to-apples)
#            UNIFIED: asl_close void-fix (asl_close(c); R("asl_close",1)); asl_search = count>0
#            asl_next loop; comments corrected (asl_close void on BOTH; asl_next returns
#            asl_object_t on BOTH macOS + rmxOS).
# build:     cc asl-harness.c -o asl-canonical  (16 deprecation warnings, 0 errors; cc rc=0)
# run:       rc=0, op116_matrix_fails=1
#
# --- mx matrix (9 R()-cases; asl_search uses count>0 asl_next loop) ---
asl_open: PASS
asl_new: PASS
asl_set: PASS
asl_log: PASS
asl_get_roundtrip: PASS
asl_set_filter: PASS
asl_log_filtered: PASS
asl_search_roundtrip: FAIL
asl_close: PASS
op116_matrix_fails=1
op116_matrix_terminal status=0
#
# asl_search_roundtrip FAIL: the count>0 asl_next loop found 0 matches in the harness's 200ms
# window (macOS unified-logging ASL store does not reliably index just-sent custom-facility
# messages for immediate asl_search). rx-x64z ran the IDENTICAL unified blob -> also FAIL ->
# VERIFIED shared-FAIL match (MATCH 9/9 incl. the shared FAIL; plausibly real write->search
# store-propagation latency on BOTH platforms, NOT a macOS-vs-rmxOS divergence).
#
# NOTE: supersedes op-117's macos-truth.out, which ran the pre-unified @794a208 harness
# (asl_close(c)==0) and reported an asl_close BUILD-FAIL "divergence (macOS void vs rmxOS
# int)". That was an overclaim: asl_close is void on BOTH (the unified harness's void-fix
# compiles on both macOS + rmxOS). The op-117 "int on rmxOS" assertion was made without
# grepping lib/libasl/asl.h. Corrected here -- asl_close is NOT a divergence.
